# About:
Files for setting up docker images and docker containers.
"requirements.pip" contains the general package requirement and "requirements-long.pip" all package requirements 
including version numbers according to "pip freeze". 